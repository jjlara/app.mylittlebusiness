<div class="container">
  <div class="row">
   <div class="col-md-6 col-md-offset-3">
     <div class="well">

  

<%= form_for @fotosprofile, :html => {:multipart => true} do |f| %>
  <% if @fotosprofile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fotosprofile.errors.count, "error") %> prohibited this fotosprofile from being saved:</h2>

      <ul>
      <% @fotosprofile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h1>Sube/Edita más Fotos de tu Negocio</h1>

  <div class="field">
    <%= f.label :mis_fotos %><br>
    <%= f.file_field :mis_fotos %>
  </div>
  <div class="actions">
    <%= f.submit "Crear/Editar", :class => "btn btn-primary" %>
  </div>
  <%= link_to 'Back', myfotosprofiles_path %>
<% end %>

</div>
</div>
</div>
</div>
